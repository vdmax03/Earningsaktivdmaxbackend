[build]
  command = "npm install && npm run build"
  publish = "dist"
  base = "earning-sakti-frontend"

[build.environment]
  NODE_VERSION = "18.19.0"

# Redirect all routes to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200